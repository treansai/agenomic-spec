{
  "description": "A `temporal_invariant` rule must carry a PLTL `formula`.",
  "match": {
    "keyword": "required",
    "instancePath": "/rules/0",
    "params": { "missingProperty": "formula" }
  }
}
