{
  "policy_id": "predicate-missing-rhs",
  "rules": [
    {
      "id": "cmp-needs-value",
      "modality": "obligation",
      "when": { "event": "tool.call.proposed" },
      "then": { "field": "risk_score", "op": "lt" }
    }
  ]
}
