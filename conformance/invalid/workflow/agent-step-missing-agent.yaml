# An `agent` step must declare which agent it invokes.
spec_version: agenomic/v0.2
workflow:
  id: workflow://example/min-flow
  name: Minimal Flow
  domain: minimal
  criticality: standard
steps:
  - id: answer
    type: agent
